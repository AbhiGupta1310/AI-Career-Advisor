# ============================================================
# Nixpacks Config for Railway
# Only sets up Nix packages — build/start commands in railway.json
# ============================================================

[phases.setup]
nixPkgs = ["nodejs_20", "python311"]
